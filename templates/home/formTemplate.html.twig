{% extends 'base.html.twig' %}

{% block body %}
    {{ parent() }}
    
    <div class='form-control form-user'>
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_widget(form) }}
    <button class='btn btn-warning' type="submit">Submit</button>
    {{ form_end(form) }}
    </div>
{% endblock %}